require('nvim-tree').setup {
    hijack_cursor = true,
    view = {
        float = {
            enable = true,
        }
    },
    renderer = {
        indent_markers = {
            enable = true,
        },
    },
}
