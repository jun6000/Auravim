require('nvim-tree').setup {
    hijack_cursor = true,
    view = {
        float = {
            enable = false,
        }
    },
    renderer = {
        indent_markers = {
            enable = true,
        },
    },
}
